# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TinyTribe' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TinyTribe
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'Firebase/Messaging'
  pod 'FirebaseCore'
  pod 'FirebaseStorage'

  target 'TinyTribeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TinyTribeUITests' do
    # Pods for testing
  end

end
